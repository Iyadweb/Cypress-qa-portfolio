describe('Find Command', () => {
    it('Should be able to apply find command', () => {
        cy.visit('https://www.letskodeit.com/practice')
        cy.get('button')
        cy.get('#open-window-example-div').find('button')
        .should('have-id','#open-window-example-div')
    })
})
